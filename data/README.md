## Data Folder

- `raw/` contains the original dataset downloaded from the UCI ML Repository.
- `processed/` contains the cleaned dataset used for analysis and dashboarding.

The cleaned dataset is the single source of truth for Power BI.
